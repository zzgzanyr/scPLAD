# Fig. 4f control-anchor swap diagnostic

Each filled marker is the mean across three random seeds. K562 is the matched target-cell-line control anchor; the remaining rows use source-cell-line anchors at generation.
